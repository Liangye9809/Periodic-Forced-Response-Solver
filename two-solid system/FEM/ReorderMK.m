function [M, K] = ReorderMK(M1, M2, K1, K2, dof1, dof2, mesh_file_1, mesh_file_2, Nc1, Nc2)
 % Nc strores the information of contact nodes, first column is the id of
 % contact nodes which needed to reorder, second and third columns are the
 % nodes to consist the node surface
 
end

